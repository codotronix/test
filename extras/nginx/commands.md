
## NGINX USEFUL COMMANDS 

- The config file is stored at and as `/etc/nginx/sites-available/default`
- After changing configs, to check for syntax error, run `nginx -t`
- To restart nginx, run `sudo systemctl restart nginx`
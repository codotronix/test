This repo contains both client side and sever side React code
i.e. It can be run both with CRA and NEXT
NEXT was introduced mainly to get static build for now, and as a way to get
SSR if ever needed in future

reactales is the static folder